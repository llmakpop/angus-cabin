# Supabase — get these from your project's Settings → API page.
# NEXT_PUBLIC_ vars are exposed to the browser; the anon key is safe there
# because Row Level Security policies on the database control access.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
